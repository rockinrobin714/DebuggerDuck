{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "babel client/src --presets=es2015,react -d dist --source-maps inline",
    "build-watch": "babel client/src --presets=es2015,react -d dist --source-maps inline -w",
    "start": "node /server/server.js"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "babel-plugin-transform-react-jsx": "^6.8.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "react": "^15.4.1",
    "react-bootstrap": "^0.30.7",
    "react-dom": "^15.4.1"
  }
}
